bcm_add_subdirectory(conn_mgr)
if("${SUBSYSTEM}" STREQUAL "host" OR "${SUBSYSTEM}" STREQUAL "opencpu")
    bcm_add_subdirectory(unix_domain)
if(NOT OPEN_SOURCE)
    bcm_add_subdirectory(trmux_direct)
    bcm_add_subdirectory(trmux_direct_dummy)
endif()
endif()
